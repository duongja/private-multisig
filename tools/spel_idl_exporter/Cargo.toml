[package]
name = "spel_idl_exporter"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1.0"
spel-framework-core = { path = "../../../spel/spel-framework-core", features = ["idl-gen"] }
